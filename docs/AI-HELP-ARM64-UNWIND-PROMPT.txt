We need help fixing a crash on Free Pascal (FPC) for ARM64 Windows: when exit (or break/continue) is used inside a try...finally block, the finally block runs but execution then faults at the first instruction after the try-finally (the "landing pad") with STATUS_ACCESS_VIOLATION—PC is correct but SP or FP appears wrong. The same pattern works on x86_64 Windows. We've tried full CONTEXT flags, handler Lr/Fp patch, bypass (RtlRestoreContext), guard-page touch; the crash persists (with bypass, Sp/Fp at fault match our context but we still fault at the landing pad). Please read docs/AI-HELP-ARM64-UNWIND.md for quick context, then docs/AI-UNWIND-LEARNINGS-SUMMARY.md for full status, attempts log (§5c), checklist (§5d), and plan (§0b). Repo: https://github.com/gig8/fpc, branch feature/win-aarch64. Propose a concrete code change, test, or diagnostic (e.g. compiler Local-SP, STATUS_UNWIND_CONSOLIDATE path, or VEH logging bytes at PC).
